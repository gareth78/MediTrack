import React from 'react';
import { Button as PaperButton, TextInput as PaperInput, Card as PaperCard } from 'react-native-paper';

export const Button = PaperButton;
export const Input = PaperInput;
export const Card = PaperCard;
